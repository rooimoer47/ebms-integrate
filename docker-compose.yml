services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: ebms
      POSTGRES_USER: ebms
      POSTGRES_PASSWORD: ebms
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
